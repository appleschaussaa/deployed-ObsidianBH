import React from "react";
import { beerData, altItems, nonAlc } from "../utils/OnTapData";
import OnTabPull from "../utils/PullTab";
// import OnTabPull from "../utils/PullTab";
// import MasonryImageList from "./components/Reusable/MasonryImageList";

// const fetchImageURL = (imgURL, size) => {
//     return `${imgURL}?w=248&fit=crop&auto=format&dpr=${size}`;
// };

// const handleButtonClick = () => {
// };

const OnTap = () => {
    return (
            <OnTabPull />

    );
};

export default OnTap;
